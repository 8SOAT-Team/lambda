name: CI/CD with Terraform for Lambda

on:
  push:
    branches:
      - main  # Gatilho para commits na branch main
  pull_request:
    branches:
      - main  # Gatilho para PRs na branch main

jobs:
  terraform:
    runs-on: ubuntu-latest

    steps:
    - name: Check out repository
      uses: actions/checkout@v2

    - name: Set up Node.js
      uses: actions/setup-node@v2
      with:
        node-version: '14'  # Use a versão do Node.js compatível com sua Lambda

    - name: Install dependencies
      run: npm install

    - name: Package Lambda Function
      run: zip lambda_function.zip index.js  # Empacota o código da Lambda

    - name: Set up Terraform
      uses: hashicorp/setup-terraform@v1
      with:
        terraform_version: '1.4.0'  # Ou outra versão desejada

    - name: Configure AWS credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: ${{ secrets.AWS_REGION }}  # Ou definir diretamente como 'us-east-1'

    - name: Initialize Terraform
      run: terraform init

    - name: Terraform Plan
      run: terraform plan

    - name: Terraform Apply
      run: terraform apply -auto-approve

    - name: Clean up
      run: rm -rf lambda_function.zip
